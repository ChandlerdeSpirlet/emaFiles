<!DOCTYPE html>
<html>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Test Details | <%= date_selected %> at <%= time_num %></title>
    <style>
        * {
            font-family: -apple-system, BlinkMacSystemFont, sans-serif; 
        }
        html {
            background-image: linear-gradient(#0095ff, #9ed7ff);
            min-height: 100%;
            -webkit-background-size: cover;
            -moz-background-size: cover;
            -o-background-size: cover;
            background-size: cover;
        }
        h1 {
            text-align: center;
        }
        h3 {
            text-align: center;
        }
        .column {
            float: left;
            height: 100%;
            padding: 10px;
            line-height: 4;
        }
        #page-container {
            position: relative;
            min-height: 90vh;
        }
        #content-wrap {
            
        }
        .row:after {
            content: "";
            display: table;
            clear: both;
        }
        .chip {
            display: inline-block;
            padding: 0 25px;
            height: 50px;
            font-size: 18px;
            line-height: 50px;
            border-radius: 25px;
            background-color: #f1f1f1;
        }
        .closebtn {
            padding-left: 10px;
            color: #888;
            font-weight: bold;
            float: right;
            font-size: 20px;
            cursor: pointer;
        }
        .closebtn:hover {
            color: #000;
        }
        button, label, input, select {
            display: block;
            font-size: 100%;
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            width: 100%;
            padding: 5px;
            height: 30px;
        }
        input[type="text"], select {
            box-shadow: inset 1px 1px 3px #ccc;
            border-radius: 5px;
        }

        label {
            margin-bottom: 5px;
        }

        button {
            width: 60%;
            margin: 0 auto;
        }
        label{
            font-weight: bold;
        }
    </style>
    <head>
        <h1><% if (level_num == -1) {%> Little Dragons 
            <% } else if (level_num == 0) { %> Basic 
            <% } else if (level_num == 1 ) { %> Level 1 
            <% } else if (level_num == 2) { %> Level 2 
            <% } else if (level_num == 3) { %> Level 3 
            <% } else if (level_num == 4) { %> Weapons
            <% } else { %> Unrecognizable Level Data <% } %></h1>
        <h3><%= time_num %></h3>
        <a href="https://emafiles.herokuapp.com/store/test_search"><button>New Search</button></a>
    </head>
    <body>
        <div id="page-container">
            <div id="content-wrap">
                <div class="row">
                    <div style="align-content: center;"class="column">
                        <% if (data) { %>
                            <% if (data.length == 0){<p> No students to display. </p>} %>
                            <% else { %>
                            <% data.forEach(function(item) { %>
                                <div class="chip">
                                    <%= item.first_name %> <%= item.last_name %> - <i><%= item.belt %></i>
                                </div>
                            <% }) %>
                            <% } %>
                        <% } else { %>
                            There are no students to display.
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>