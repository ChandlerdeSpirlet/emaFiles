<!DOCTYPE html>
<html>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Class Details | <%= date_selected %> at <%= time_num %></title>
    <style>
        * {
            font-family: -apple-system, BlinkMacSystemFont, sans-serif; 
        }
        html {
            background-image: linear-gradient(#0095ff, #9ed7ff);
            -webkit-background-size: cover;
            -moz-background-size: cover;
            -o-background-size: cover;
            background-size: cover;
        }
        h1 {
            text-align: center;
        }
        h3 {
            text-align: center;
        }
        .column {
            float: left;
            height: 100%;
            padding: 10px;
        }
        #page-container {
            position: relative;
            min-height: 90vh;
        }
        #content-wrap {
            
        }
        .row:after {
            content: "";
            display: table;
            clear: both;
        }
        .chip {
            display: inline-block;
            padding: 0 25px;
            height: 50px;
            font-size: 18px;
            line-height: 50px;
            border-radius: 25px;
            background-color: #f1f1f1;
            padding-bottom: 5px;
        }
        .closebtn {
            padding-left: 10px;
            color: #888;
            font-weight: bold;
            float: right;
            font-size: 20px;
            cursor: pointer;
        }
        .closebtn:hover {
            color: #000;
        }
        button, label, input, select {
            display: block;
            font-size: 100%;
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            width: 100%;
            padding: 5px;
            height: 30px;
        }
        input[type="text"], select {
            box-shadow: inset 1px 1px 3px #ccc;
            border-radius: 5px;
        }

        label {
            margin-bottom: 5px;
        }

        button {
            width: 60%;
            margin: 0 auto;
        }
        label{
            font-weight: bold;
        }
        #footer {
                position: absolute;
                bottom: 0;
                width: 100%;
                height: 2.5rem;
            }
    </style>
    <head>
        <h1><% if (level_num == 0) {%> Little Dragons 
            <% } else if (level_num == 0.5) { %> Basic 
            <% } else if (level_num == 0.8) { %> Basic, Floor 2 
            <% } else if (level_num == 1 ) { %> Level 1 
            <% } else if (level_num == 1.5) { %> Level 1, Floor 2 
            <% } else if (level_num == 2) { %> Level 2 
            <% } else if (level_num == 3) { %> Level 3 
            <% } else if (level_num == 3.5) { %> Prep Class
            <% } else if (level_num == 4) { %> Black Belt
            <% } else if (level_num == 6) { %> Weapons
            <% } else if (level_num == 4) { %> Open Mat
            <% } else { %> Unrecognizable Level Data <% } %></h1>
        <h3><%= time_num %></h3>
    </head>
    <body>
        <div id="page-container">
            <div id="content-wrap">
                <div class="row">
                    <div style="align-content: center;"class="column">
                        <% if (data) { %>
                            <% data.forEach(function(item) { %>
                                <div class="chip">
                                    <%= item.first_name %> <%= item.last_name %>
                                </div>
                            <% }) %>
                        <% } else { %>
                            There are no students to display.
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
        <footer id="footer">
            <a href="https://emafiles.herokuapp.com/store/class_lookup"><button>New Search</button></a>
        </footer>
    </body>
</html>